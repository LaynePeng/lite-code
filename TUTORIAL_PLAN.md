# 教程重组方案：从 22 课到 24 课

## 背景

对比 v0.13.0 → v0.14.0 之间的增量功能，现有 22 课教程未覆盖的新内容有 6 个独立知识点。按「知识篇 / 实战篇」分篇原则与知识分类清晰原则重组为 **24 课**：

- **知识篇（第 1-13 课）**：原 15 门知识课合并为 12 门，末尾新增 1 门综合复盘；
- **实战篇（第 14-24 课）**：原 7 门实战课保留，插入 4 门新课，覆盖全部新增知识点。

## 内容组织原则

### 分篇原则

- **第 1-13 课（知识篇）以知识为主**：讲机制、原理、设计权衡与协议规范；
- **第 14-24 课（实战篇）以工程实战为主**：手写实现、完整代码、集成落地与踩坑复盘。

### 知识篇代码策略（第 1-13 课）

1. **保留最小可运行示意代码**：能说明原理即可（如 L1 的流累加器核心逻辑），不放完整生产实现；
2. **"指向"代替"粘贴"**：需要完整实现时写"完整代码见第 X 课"，不在知识篇贴大段生产代码；
3. **代码只服务"为什么"**：每段代码有明确教学目的（演示机制/权衡），否则删除；
4. **工程细节下沉**：具体 API 端点、前端组件、节流参数、字段名等一律留到实战篇对应课程。

## 新课表

### 模块一：ReAct 与循环（第 1-5 课）— 不变

| 编号 | 课程 |
|---|---|
| 1 | LLM 原生接口封装与 Tool Calling 底层流式解析 |
| 2 | Agent 控制循环的异常容错、死循环中断与 Token 预算控制 |
| 3 | 上下文管理、Token 动态计算与 Dynamic System Prompt |
| 4 | Prompt 缓存机制：命中率优化与工程实践 |
| 5 | Token 节省策略：从 OpenSquilla 到缓存友好 |

### 模块二：代码理解与安全操作（第 6-9 课）— 原 7 课合并为 4 课

| 编号 | 课程 | 来源 |
|---|---|---|
| 6 | 代码理解：检索机制与结构分析 | 原 6（代码感知/Ripgrep）+ 原 7（Tree-sitter AST） |
| 7 | 安全代码操作：执行隔离与原子编辑 | 原 8（沙箱）+ 原 9（精确编辑/Patch） |
| 8 | Skills 系统：注入机制与权限治理 | 原 10（Skills）+ 新增 Skills 高级管理 |
| 9 | 标准 Model Context Protocol（MCP）接入 | 原 11 |

### 模块三：核心架构与多 Agent（第 10-12 课）— 原 5 课合并为 3 课

| 编号 | 课程 | 来源 |
|---|---|---|
| 10 | 插件架构：内核设计与依赖注入 | 原 12（Cordis 内核）+ 原 13（DI/生命周期） |
| 11 | 多 Agent 协作：编排、通信与持久化 | 原 14（子 Agent 调度）+ 新增子 Agent 增强 |
| 12 | Agent 类型与自定义机制 | 原 15 |

### 模块四：知识篇收尾（第 13 课）

| 编号 | 课程 |
|---|---|
| 13 | 知识篇综合复盘：从机制到架构的完整图谱 |

### 模块五：手写实战 lite-code（第 14-24 课）— 11 课

| 编号 | 课程 | 来源 |
|---|---|---|
| 14 | 核心 Core 内核编码（实战第一篇） | 原 16 |
| 15 | LLM 多供应商适配器与核心工具集（实战第二篇） | 原 17 |
| 16 | AgentLoop 主循环与增强集成（实战第三篇） | 原 18 |
| **17** | **【新】AgentLoop 扩展：工具并行化、排队输入、LLM 故障重试** | 新增 |
| 18 | 安全沙箱与高危拦截实战（实战第四篇） | 原 19 |
| **19** | **【新】人机交互：审批卡与 ask_user 提问** | 新增 |
| **20** | **【新】TODO 清单系统：工具插件、事件驱动与持久化** | 新增 |
| 21 | Web UI 实战（实战第五篇） | 原 20 |
| **22** | **【新】会话管理与综合设置：孤儿会话、项目切换、配置面板** | 新增 |
| 23 | Electron 桌面应用（实战第六篇） | 原 21 |
| 24 | 工程实践与踩坑（实战终章） | 原 22 |

## 知识篇合并大纲

### 第 6 课「代码理解：检索机制与结构分析」（原 6 + 原 7）

1. 大型代码库的检索难题：全量扫描 vs 索引（倒排 / 符号表）的取舍
2. Tree-sitter 增量解析原理：为何 AST 优于正则（语法正确性、嵌套结构）
3. 符号表与依赖图构建：从语法树到调用关系
4. 代码感知工具链的边界：索引常驻 vs 按需扫描
5. 工程意义：上下文有限时如何"找到对的文件、读懂对的函数"
（最小示例：提取函数签名 / 文件树过滤的 10-15 行示意代码）

### 第 7 课「安全代码操作：执行隔离与原子编辑」（原 8 + 原 9）

1. 威胁模型：破坏性指令 / 资源耗尽 / 数据外泄
2. OS 隔离原语：namespace / seccomp / cgroup 的能力边界；超时与资源限制
3. 编辑原子性：diff 语义、事务性替换、失败自愈（补丁格式设计动机）
4. 能力最小化原则：白名单、环境变量擦除、路径穿越防护
（最小示例：路径穿越检查 / 高危命令过滤的 8-12 行示意代码）

### 第 8 课「Skills 系统：注入机制与权限治理」（原 10 + 新增）

1. 项目指令注入：AGENTS.md / CLAUDE.md 的优先级与上下文代价
2. 索引常驻 + 按需加载：省 Token 的工程权衡
3. 权限三级语义：allow / deny / ask，glob 通配规则（插入序首个命中）
4. deny 的完整隐藏链：索引不列出、triggers 不匹配、load_skill 拦截
5. triggers 匹配模式：substring vs advanced（词边界 / 正则包裹）
6. 工程意义：减少误注入 = 省 Token + 提高遵循率
（最小示例：权限匹配器骨架 12 行）

### 第 9 课「标准 Model Context Protocol（MCP）接入」（原 11）

1. 协议动机：为什么需要统一工具协议
2. JSON-RPC 生命周期：initialize / tools/list / tools/call / 通知
3. 与内置工具的信任差异：外部进程的安全边界
4. 传输方式：stdio / SSE 的选择

### 第 10 课「插件架构：内核设计与依赖注入」（原 12 + 原 13）

1. 微内核 vs 单体：空间解耦（Spatiotemporal Composability）
2. 洋葱模型中间件：beforeLLM / beforeTool / afterTool 三阶段管道
3. 依赖注入与服务定位：内核服务容器
4. 插件生命周期：install / 启动 / 卸载钩子

### 第 11 课「多 Agent 协作：编排、通信与持久化」（原 14 + 新增）

1. 编排模式：父子调度、扇出与结果聚合
2. 事件隔离与跨总线转发（与 L1 流式解码的区别：转发 vs 解码）
3. 审批透传：共享 ApprovalGate vs 独立门的设计
4. 内存态 vs 持久化：会话级状态为何要落盘、如何恢复
5. 配套取舍：子 Agent 暂不开放 ask_user 的设计权衡

### 第 12 课「Agent 类型与自定义机制」（原 15）

1. 策略与能力分离：Build（全量）/ Plan（只读）
2. 工具裁剪与权限对照
3. 用户扩展：注册自定义 Agent 的接口设计

### 第 13 课「知识篇综合复盘」

1. 一条主线：从"循环怎么转"到"能力怎么加"到"架构怎么活"
2. 知识图谱总览：模型层 / 能力层 / 编排层 / 界面层四层架构
3. 各课知识点与 lite-code 项目模块的对应关系（为实战篇预热）

## 实战篇新增四课大纲

### 第 17 课「AgentLoop 扩展：工具并行化、排队输入、LLM 故障重试」

1. 回顾主循环结构（L16）
2. 写类工具精细并行化：从"含写整轮串行"到"写类串行 + 只读并行"；三种模式 never/always/auto；顺序依赖风险分析；结果按原序回填
3. 执行中补充指令（排队输入）：TaskHandle 队列设计；AgentLoop 回合开始前注入；前端"已入队"标记 + 确认提示
4. LLM 瞬时故障自动重试：指数退避、重试上限；可重试 vs 不可重试错误；llm:retry 事件前端展示
5. 测试设计：并行化四用例 / 排队注入 / 重试次数

### 第 19 课「人机交互：审批卡与 ask_user 提问」

1. Agent 为什么需要"暂停等待用户"：asyncio.Future 挂起模型
2. 回顾 ApprovalGate（L18 安全课已讲触发点）
3. QuestionGate：与 ApprovalGate 的异同（选项列表 + 自定义输入；回答是字符串而非 bool）
4. ask_user 工具：工具插件定义；工厂函数模式 make_ask_user_handler 注入真实事件总线
5. 前端提问条：非阻塞 inline 设计（提问条固定在输入区上方，不遮挡对话、可滚动查看上下文）；多问题 Tab、选项点选、自定义输入
6. 对比：审批（二值决策）vs 提问（开放信息收集）的工程差异
7. Plan Agent 的 ask_user 支持：只读白名单为何开放提问（default_plan_agent 工具集含 ask_user）；create_kernel 对裁剪后 registry 的 handler 注入（app.py:414-419）；Build（全量）vs Plan（只读 + 提问）的权限对照

### 第 20 课「TODO 清单系统：工具插件、事件驱动与持久化」

1. ToolPlugin 完整生命周期：install → get_tools → execute
2. todo_write 工具设计：全量覆盖式、校验、渲染板面
3. 事件驱动实时推送：todo:updated → 前端 TODOs 面板；ContextVar 传递 current_session_id
4. 看板持久化：原子写入、内存命中优先、磁盘恢复、会话删除时清理
5. 前端 TODOs 面板：进度统计、in_progress 高亮、任务结束保留
6. 与 AgentLoop 的绑定/解绑时机（TaskManager）

### 第 22 课「会话管理与综合设置」

1. 会话与项目绑定回顾（L14 SessionStore）
2. 孤儿会话治理：项目目录不存在 → 列表不显示；DELETE /api/sessions/cleanup 批量清理
3. 双击会话切项目：metadata.workspace 读取 → POST /api/workspace 切换 → 重开会话
4. 设置弹窗「综合设置」Tab：会话清理按钮、zip 上限配置
5. MCP 保存后工具列表自动刷新

### 第 21 课「Web UI 实战」补充大纲（工具按 Agent 动态化）

1. 工具列表按 Agent 动态加载：/api/tools?agent_id=xxx 端点设计；不传 agent_id 时保持全量（向后兼容）
2. Tab 键切换 Agent：前端键盘监听（App.tsx useEffect + keydown）与主 Agent 轮转逻辑（跳过 subagent）
3. 切换后联动刷新：工具列表 / 系统提示词 / 权限集随 currentAgent 更新
4. 边界处理：primary agent < 2 时不响应 Tab；切换发生在任务未运行期间

## 实践篇去重方案

实践篇（模块五）的定位是"把前面的理论组装成完整产品"，重复不可避免，但**每处重复必须带增量**。原则：实践篇代码必须有增量信息，纯重复部分用指针指向知识篇，把篇幅让给真正的新内容（并行化、ask_user、TODO、会话管理等 4 门新课）。

| 课（新编号） | 冗余点 | 重复对象 | 去重方案 |
|---|---|---|---|
| L15（原17）LLM 适配器与工具集 | §2 手写 SSE 解析器整段 | L1 整课 | 改为"简述 + 指向 L1 + 只写增量"（usage 提取、Anthropic 协议差异） |
| L15（原17） | §6 核心工具集实现（BlockReplacer/ShellTools/FileTreeWalker） | L6/L7 | 只给注册表机制 + 每个工具 3-5 行签名，实现指向知识篇 |
| L16（原18）AgentLoop | §2-3 防御机制集成（safe_json_parse/StateTracker/truncator/ContextManager） | L2/L3 | 改为"集成清单表格 + 每个机制 2-3 行代码 + 指向知识篇"，篇幅让给 FSM 本身 |
| L18（原19）安全沙箱 | 高危命令拦截（DANGEROUS_PATTERNS/白名单） | L7 | 只写相对 L7 的增量：动态热加载、白名单绕过修复、MCP 一刀切审批 |
| L24（原22）工程实践 | §1 模型空响应（empty_reply_retries 实现） | L16 §D3 | 改为"现象→根因→指向 L16 已实现"；保留 L16 没有的坑（配置迁移、会话 ID 碰撞、一等状态、强制交付要求） |

## 迁移映射

| 原编号 | 新编号 | 变动 |
|---|---|---|
| 1-5 | 1-5 | 不变 |
| 6 | 6 | 与原 7 合并 |
| 7 | 6 | 并入（AST → 代码理解） |
| 8 | 7 | 与原 9 合并 |
| 9 | 7 | 并入（Patch → 安全操作） |
| 10 | 8 | 与新增 Skills 管理合并 |
| 11 | 9 | 不变 |
| 12 | 10 | 与原 13 合并 |
| 13 | 10 | 并入（DI → 插件架构） |
| 14 | 11 | 与新增子 Agent 增强合并 |
| 15 | 12 | 不变 |
| — | 13 | 新增综合复盘 |
| 16 | 14 | +0（实战篇顺延） |
| 17 | 15 | +0 |
| 18 | 16 | +0 |
| — | 17 | 新增 AgentLoop 扩展 |
| 19 | 18 | +1 |
| — | 19 | 新增 ask_user |
| — | 20 | 新增 TODO |
| 20 | 21 | +1 |
| — | 22 | 新增会话管理 |
| 21 | 23 | +2 |
| 22 | 24 | +2 |

## 引用修改策略

1. 所有 `AI-Coding-Agent-Dummy-Book/*.md` 中的"第 N 课"按映射表批量替换
2. 文件名前缀 `NN-` 按新编号重命名
3. `README.md` 课表全表更新
4. 各课开头/结尾的模块过渡段人工复核（注意合并课要重写过渡段）
5. 知识篇按代码策略瘦身：大段生产代码改为"指向实战篇"
